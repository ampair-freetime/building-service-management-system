# PLANNING.md — ระบบ Lost & Found: Search และ Claim

เอกสารนี้เป็น **คู่มือออกแบบ (design guide)** สำหรับพัฒนา 2 ฟีเจอร์ในระบบของหาย-ของพบ
เขียนให้อ่านได้โดยไม่ต้องมีความรู้เดิมเกี่ยวกับโปรเจกต์ อธิบายทีละขั้นตามลำดับการทำงานจริง

---

## 0. วิธีอ่านเอกสารนี้

### ขอบเขตงานของฉัน

> **ฉันรับผิดชอบเฉพาะ Backend ฝั่ง guest เท่านั้น**
> คือ endpoint ใต้ `/api/v1/guest/*` และไฟล์ model / schema / service / form ที่รองรับมัน

ส่วนของ staff (เจ้าหน้าที่) จะอธิบายไว้ใน [ส่วนที่ 3](#ส่วนที่-3-ฝั่ง-staff-อธิบายเพื่อให้เข้าใจภาพรวม) **เพื่อให้เข้าใจว่าข้อมูลที่เราส่งไปถูกเอาไปทำอะไรต่อ** ไม่ใช่เพื่อให้ไปเขียนเอง

### สัญลักษณ์ที่ใช้

| สัญลักษณ์ | ความหมาย |
|---|---|
| 🟢 | งานของฉัน — guest backend |
| 🔵 | งานของคนอื่น — staff backend / frontend (อธิบายเป็นบริบทเท่านั้น) |
| ⚠️ | จุดที่พลาดแล้วเกิดช่องโหว่หรือบั๊กจริง ต้องอ่านให้เข้าใจ |
| 🔗 | สิ่งที่ต้องรอฝั่งอื่นเสร็จก่อน ถึงจะทดสอบครบวงจรได้ |

### คำศัพท์ที่จะเจอบ่อย

| คำ | ความหมายในบริบทนี้ |
|---|---|
| **guest** | ผู้ใช้ทั่วไปที่ไม่มีบัญชีในระบบ ไม่ต้อง login |
| **clerk** | เจ้าหน้าที่ธุรการ เป็นคนตรวจสอบและอนุมัติประกาศ (`StaffRole.CLERK`) |
| **item / ประกาศ** | หนึ่งแถวในตาราง `lost_items` เป็นได้ทั้งประกาศ "ของหาย" และ "พบของ" |
| **claim** | คำร้องขอรับของคืน หนึ่งแถวในตาราง `lost_claims` |
| **item_code** | รหัสติดตามที่อ่านง่าย เช่น `FOUND-20260906-A1B2C3D4` ระบบสร้างให้ตอนแจ้ง |
| **endpoint** | หนึ่ง URL + HTTP method ที่ API เปิดให้เรียก |
| **schema** | คลาส Pydantic ที่กำหนดรูปทรงข้อมูลเข้า-ออก และตรวจความถูกต้องให้อัตโนมัติ |
| **service** | ชั้นที่เก็บ business logic แยกจาก endpoint เพื่อให้เทสต์และใช้ซ้ำได้ |

---

## 1. ภาพรวมระบบก่อนลงรายละเอียด

### 1.1 มีใครในระบบบ้าง

```
   ┌─────────┐        ┌──────────────┐        ┌─────────┐
   │  guest  │───────▶│   Backend    │◀───────│  clerk  │
   │(ไม่ login)│        │   FastAPI    │        │ (login) │
   └─────────┘        └──────┬───────┘        └─────────┘
                             │
                    ┌────────┴────────┐
                    │   PostgreSQL    │
                    │  + Cloudflare R2│ (เก็บรูป)
                    └─────────────────┘
```

### 1.2 วงจรชีวิตของประกาศหนึ่งใบ

ตาราง `lost_items` มีคอลัมน์ `status` ซึ่งเป็น enum `LostStatus` มี 5 ค่า
การเปลี่ยนสถานะเรียกว่า **state machine** (เครื่องสถานะ) — คือกฎว่าจากสถานะหนึ่งไปสถานะไหนได้บ้าง

```
        guest แจ้ง
            │
            ▼
       ┌─────────┐   clerk ปฏิเสธ   ┌──────────┐
       │ PENDING │─────────────────▶│ REJECTED │  (จบ)
       └────┬────┘                  └──────────┘
            │ clerk อนุมัติ
            ▼
       ┌──────────┐  ◀── ตรงนี้เท่านั้นที่คนทั่วไปมองเห็นประกาศ
       │ APPROVED │  ◀── และตรงนี้เท่านั้นที่ claim ได้
       └────┬─────┘
            │ clerk ยืนยันว่าส่งของคืนเจ้าของแล้ว
            ▼
       ┌─────────┐
       │ CLAIMED │  (จบ)
       └─────────┘

       ┌─────────┐
       │ CLOSED  │  ปิดโดยไม่มีคนมารับ เช่น หมดอายุเก็บ
       └─────────┘
```

⚠️ **จุดที่ต้องจำ:** `PENDING` แปลว่า "ยังไม่มีใครนอกจากผู้แจ้งมองเห็น" ทุก endpoint ฝั่ง public
ต้องกรอง `status == APPROVED` เสมอ ยกเว้นโหมดติดตามที่พิสูจน์ตัวตนด้วยอีเมลแล้ว

### 1.3 สองฟีเจอร์ในเอกสารนี้อยู่ตรงไหนของวงจร

```
  guest แจ้งพบของ ──▶ PENDING ──▶ [clerk อนุมัติ] ──▶ APPROVED
                                                          │
                                          ┌───────────────┴──────────────┐
                                          │                              │
                                   【ส่วนที่ 1】                    【ส่วนที่ 2】
                                     Search                          Claim
                              guest ค้นหาว่ามีของ           guest ยื่นคำร้องว่า
                                 ตัวเองไหม                    "ของชิ้นนี้ของฉัน"
```

---

# ส่วนที่ 1: ระบบ Search

## 1.1 โจทย์

guest ที่ทำของหายเข้าเว็บมาแล้วอยากรู้ว่า **"มีใครเก็บของฉันได้แล้วหรือยัง"**
เขาจะพิมพ์คำอธิบายของที่จำได้ เช่น `กระเป๋าสีน้ำตาล` หรือ `บัตรนักศึกษา อาคาร 3`
แล้วคาดหวังว่าจะเจอประกาศที่ตรงกัน ถ้ามี

endpoint ที่รับผิดชอบคือ:

```
GET /api/v1/guest/lost-items      ← ประกาศตามหาของ
GET /api/v1/guest/found-items     ← ประกาศพบของ
```

ทั้งคู่เรียก service ตัวเดียวกันคือ `list_public_items()` ใน `app/services/lost_found.py`

## 1.2 ข้อเท็จจริงที่กำหนดคำตอบทั้งหมด: ข้อมูลเป็นภาษาไทย

ปกติเวลาพูดถึง "ระบบค้นหาข้อความ" คำตอบมาตรฐานคือ **Full-Text Search (FTS)**
คือกลไกที่ฐานข้อมูลตัดข้อความเป็นคำ ๆ เก็บเป็นดัชนี แล้วค้นแบบ "ตรงคำ" ได้เร็วมาก
ใน PostgreSQL ใช้ชนิดข้อมูล `tsvector` (เก็บคำที่ตัดแล้ว) คู่กับ `tsquery` (คำค้น)

**แต่ FTS ใช้กับภาษาไทยไม่ได้** เพราะตัวตัดคำ (parser) ของ PostgreSQL แบ่งคำด้วย
ช่องว่างและเครื่องหมายวรรคตอน ส่วนภาษาไทยเขียนติดกันไม่มีช่องว่างคั่นคำ:

```sql
to_tsvector('simple', 'โทรศัพท์สีดำหายที่โรงอาหาร')
-- ได้ token เดียว: 'โทรศัพท์สีดำหายที่โรงอาหาร'
-- → ค้นคำว่า 'โทรศัพท์' ไม่เจอ
```

และ PostgreSQL **ไม่มี text search configuration สำหรับภาษาไทย** ติดมาให้
จะทำได้ต้องเอา library ตัดคำไทย (เช่น PyThaiNLP) มาตัดตอนบันทึกแล้วเก็บ `tsvector` เอง
ซึ่งเป็นงานใหญ่และต้อง re-index ทั้งตารางทุกครั้งที่เปลี่ยน dictionary

> ✅ **ข้อสรุป:** วิธีที่โค้ดปัจจุบันใช้อยู่ — `ILIKE '%คำค้น%'` (การหาสตริงย่อย)
> **ไม่ใช่ทางเลือกมักง่าย แต่เป็นทางเลือกที่ถูกต้องสำหรับภาษาไทย**
> เพราะมันหา "ลำดับอักขระย่อย" โดยไม่สนใจขอบเขตคำ ตรงกับธรรมชาติของภาษาไทยพอดี
>
> ทิศทางคือ **ปรับปรุงของเดิม ไม่ใช่เปลี่ยนไปใช้ FTS**

### ทำไม `ILIKE '%...%'` ถึงช้าเมื่อข้อมูลเยอะ

`%` ข้างหน้าคำค้นเรียกว่า **leading wildcard** และมันทำให้ **B-tree index ใช้ไม่ได้เลย**

B-tree เรียงข้อมูลตามตัวอักษรจากซ้ายไปขวา เหมือนพจนานุกรม — ถ้ารู้ว่าคำขึ้นต้นด้วย "กร"
ก็เปิดไปหน้านั้นได้เลย แต่ถ้าโจทย์คือ "คำที่มี 'ระเป' อยู่ตรงไหนก็ได้" พจนานุกรมช่วยไม่ได้
ต้องอ่านทุกหน้า ซึ่งในฐานข้อมูลเรียกว่า **Sequential Scan** (อ่านทุกแถว)

index ที่มีอยู่บน `lost_items` ตอนนี้คือ `report_type`, `status`, `reporter_email`,
`created_at` และ composite `(report_type, status)` — **ไม่มีตัวไหนช่วย search ได้เลย**

## 1.3 สถาปัตยกรรมที่เลือก: backend ตัดขอบเขต / frontend ค้นในขอบเขต

แทนที่จะถามว่า "ใครควรค้น" ให้ถามว่า **"เงื่อนไขแบบไหนที่ฐานข้อมูลทำได้ดี"**

| ประเภทเงื่อนไข | ฐานข้อมูลทำได้แค่ไหน | ควรอยู่ที่ไหน |
|---|---|---|
| `status = approved`, `report_type = found`, `deleted_at IS NULL`, ช่วงวันที่ | **ดีมาก** ใช้ B-tree index ที่มีอยู่แล้ว | 🟢 **backend** |
| `ILIKE '%กระเป๋าสีดำ%'` ข้ามหลายคอลัมน์ | **แย่** ต้อง Seq Scan ทุกครั้ง | 🔵 **frontend** |

เส้นแบ่งอยู่ตรงนี้พอดี: ให้ backend ทำเฉพาะเงื่อนไขที่ *ฟรี* (มี index รองรับอยู่แล้ว)
แล้วโยนเงื่อนไขที่ *แพงที่สุด* ตัวเดียว คือ free-text ไปให้ frontend
ซึ่ง `.includes()` บน array ในหน่วยความจำเร็วอยู่แล้ว และภาษาไทยก็ไม่มีปัญหา
(`"กระเป๋าสะพาย".includes("กระเป๋า")` ทำงานเหมือน `ILIKE` เป๊ะ)

ผลลัพธ์: **ไม่ต้องลง extension, ไม่ต้อง migration, ไม่ต้องแตกคำ**

```
GET /api/v1/guest/found-items?days=90&limit=500
        │
        │  🟢 backend: filter ที่ใช้ index ได้ทั้งหมด ไม่มี ILIKE เลย
        │     report_type + status=APPROVED + deleted_at IS NULL + created_at >= ...
        ▼
   { items: [...], total: N, limit: 500, offset: 0 }
        │
        │  🔵 frontend เช็ค: items.length === total ?
        │
        ├─ ครบ ──▶ กรองใน JS ได้เต็มที่ ผลถูกต้อง 100% ลื่น ไม่ยิง network
        │
        └─ ไม่ครบ ▶ บอกผู้ใช้ให้แคบช่วงเวลาลง หรือ fallback ไปใช้ ?search= ของ backend
```

## 1.4 กุญแจสำคัญ: "สัญญาความครบถ้วน"

ปัญหาเดียวของการกรองที่ frontend คือ **frontend อาจถือข้อมูลไม่ครบ**

ลองนึกภาพ: มีประกาศ approved 250 รายการ frontend โหลดมาแค่ 20 (ค่า `limit` เริ่มต้น)
guest พิมพ์ "กระเป๋า" → JS กรองใน 20 รายการนั้น → เจอ 1 → ขึ้นว่า "พบ 1 รายการ"
**แต่กระเป๋าของเขาอยู่รายการที่ 137 ซึ่งมองไม่เห็นตลอดกาล** → เขาสรุปว่าไม่มีใครเก็บได้แล้วเลิกหา

⚠️ นี่คือความล้มเหลวตรงจุดที่ฟีเจอร์นี้มีไว้เพื่อป้องกันพอดี และ **ผลลัพธ์ผิดแบบเงียบ ๆ
อันตรายกว่าผลลัพธ์ที่ช้า** เพราะผู้ใช้ไม่มีทางรู้ว่าตัวเองพลาดอะไรไป

**ทางแก้:** API คืน `total` มาให้อยู่แล้ว (ดู `GuestItemListResponse`) frontend จึงตรวจได้ตรง ๆ

```
items.length === total  →  ถือครบ  →  กรองเองได้ ผลถูกต้องแน่นอน
items.length <  total   →  ถือไม่ครบ →  ห้ามกรองเอง
```

สิ่งนี้เปลี่ยน "frontend filtering ที่ผลอาจผิด" ให้เป็น **"frontend filtering ที่พิสูจน์ได้ว่าถูก"**

## 1.5 🟢 งานที่ต้องทำฝั่ง backend (3 จุด)

### จุดที่ 1 — เพิ่มตัวตัดขอบเขตด้วยช่วงเวลา

เพิ่ม query parameter ใหม่ในทั้ง `lost_item.py` และ `found_item.py`:

```python
days: Annotated[int, Query(ge=1, le=365)] = 90
```

แล้วส่งต่อไป `list_public_items()` เพื่อเติม filter:

```python
filters.append(LostItem.created_at >= datetime.now(UTC) - timedelta(days=days))
```

**ทำไมเลือกช่วงเวลา:**
- `created_at` **มี index อยู่แล้ว** (`ix_lost_items_created_at`) → ฟรี ไม่ต้องเพิ่มอะไร
- ตรงพฤติกรรมจริง — คนหาของจำได้ว่า "หายเมื่อประมาณสัปดาห์ที่แล้ว"
- **จำกัดขนาดชุดได้จริง** ต่างจาก pagination ที่แค่ซอยชุดใหญ่เป็นชิ้นเล็ก แต่ชุดยังใหญ่เท่าเดิม

### จุดที่ 2 — ขยายเพดาน `limit`

ตอนนี้คือ `Query(ge=1, le=100)` ต้องขยายให้พอกับหนึ่งขอบเขต เช่น `le=500`
แล้วปล่อยให้ `total` เป็นตัวบอกความจริงว่าเกินหรือไม่

### จุดที่ 3 — ⚠️ เอา `custody_location` ออกจาก list response

ข้อนี้ **ไม่ใช่ทางเลือก แต่เป็นเงื่อนไขของสถาปัตยกรรมนี้**

`custody_location` คือ "ของชิ้นนี้ถูกเก็บไว้ที่ไหน" เช่น `ห้องประชาสัมพันธ์`
พอ frontend ถือข้อมูลทั้งชุด ข้อมูลนี้จะไปนั่งรวมกันในเบราว์เซอร์ของทุกคน
กลายเป็น **แผนที่บอกว่าของมีค่าทุกชิ้นอยู่ที่ไหน** ให้คนไปรับของที่ไม่ใช่ของตัวเอง

เอาออกจาก `GuestItemPublicResponse` แล้วไปเปิดเผยเฉพาะตอนที่ claim ได้รับอนุมัติแล้ว
(ดู [ส่วนที่ 2.6](#26-custody_location--ข้อมูลที่ปลดล็อกด้วยการอนุมัติ))

### สิ่งที่ **ไม่ต้อง** ทำ

- `?search=` เดิม **ไม่ต้องแตะ** เก็บไว้เป็น fallback เมื่อชุดข้อมูลใหญ่เกิน
- ไม่ต้องลง `pg_trgm`, ไม่ต้องทำ FTS, ไม่ต้องทำ fuzzy search
- ⚠️ **ห้ามค้นใน `private_verification_detail` เด็ดขาด** — เป็นข้อมูลลับที่ใช้ยืนยัน
  ความเป็นเจ้าของ ถ้าค้นได้ = คนร้ายยิงคำค้นไปเรื่อย ๆ จนเดาคำตอบได้

## 1.6 🟢 เทสต์ที่ควรเขียน (`tests/test_guest_lost_found.py`)

1. ไม่ส่ง `days` → ใช้ค่าเริ่มต้น 90 วัน และผลลัพธ์เดิมยังถูกต้อง
2. ประกาศที่ `created_at` เก่ากว่าช่วงที่ขอ → ไม่อยู่ในผลลัพธ์
3. `days` นอกช่วง (0 หรือ 400) → 422
4. `limit=500` → ผ่าน validation (พิสูจน์ว่าขยายเพดานแล้วจริง)
5. `total` ตรงกับจำนวนทั้งหมดในขอบเขต ไม่ใช่แค่จำนวนที่คืนในหน้านั้น
6. `custody_location` **ไม่อยู่** ใน response ของ list endpoint

---

# ส่วนที่ 2: ระบบ Claim (ขอรับของคืน)

## 2.1 Claim คืออะไร และทำไมต้องมี

สมมติสถานการณ์จริง:

```
1. สมชายเก็บกระเป๋าสตางค์ได้ที่โรงอาหาร → แจ้งเข้าระบบเป็นประกาศ "พบของ"
   ตอนแจ้ง ระบบถามว่า "มีอะไรที่เจ้าของตัวจริงเท่านั้นที่รู้"
   สมชายกรอกว่า "ข้างในมีบัตรนักศึกษาชื่อ ก. และรูปสุนัขพันธุ์ชิวาวา"
                                  ↑ เก็บใน private_verification_detail (ความลับ)

2. clerk อนุมัติประกาศ → ขึ้นเว็บให้คนทั่วไปเห็น
   แต่เว็บแสดงแค่ "กระเป๋าสตางค์สีน้ำตาล พบที่โรงอาหาร" ← ไม่บอกความลับ

3. สมหญิงทำกระเป๋าหาย มาค้นเจอประกาศนี้ → กด "นี่ของฉัน" แล้วอธิบายว่า
   "ข้างในมีบัตรนักศึกษาของฉัน กับรูปหมาชิวาวาที่เลี้ยงไว้"
                                  ↑ เก็บใน proof_detail

4. clerk เปิดดูทั้งสองอันเทียบกัน → ตรงกัน → อนุมัติ
                                   → ไม่ตรง → ปฏิเสธ

5. สมหญิงไปรับของที่จุดรับฝาก → clerk กดยืนยันส่งมอบ → จบ
```

**หัวใจของกลไกนี้คือ:** ผู้พบเขียนความลับไว้ ผู้อ้างสิทธิ์ต้องบรรยายได้ตรงกันจากความจำ
ถ้าระบบเผลอเปิดเผยความลับให้ใครก็ตามเห็น **กลไกทั้งหมดพังทันที** เพราะใครก็ลอกไปตอบได้

## 2.2 ⚠️ กฎความปลอดภัยข้อเดียวที่สำคัญที่สุด

> **`private_verification_detail` ต้องไม่ออกจาก backend ไปหา guest ในทุกกรณี**

ตอนนี้ป้องกันไว้แล้วด้วยเทคนิคที่เรียกว่า **"ป้องกันด้วยรูปทรงของ schema"**
คือ `GuestItemPublicResponse` **ไม่มีฟิลด์นี้อยู่เลยตั้งแต่แรก** — ไม่ใช่การมีแล้วค่อยลบทีหลัง

เทคนิคนี้ปลอดภัยกว่ามาก เพราะถ้าใช้วิธี "คืนทุกฟิลด์แล้วลบตัวที่ไม่อยากให้เห็น"
วันหนึ่งมีคนเพิ่มฟิลด์ลับใหม่เข้า model แล้วลืมไปเพิ่มในรายการที่ต้องลบ ข้อมูลก็หลุดทันที
แต่ถ้าใช้วิธีปัจจุบัน ฟิลด์ใหม่จะไม่หลุดเอง เพราะต้องไปเพิ่มใน schema ก่อนถึงจะออกมา

🟢 **สิ่งที่ต้องทำ:** schema ทุกตัวที่จะสร้างใหม่ในส่วน claim ต้องยึดหลักเดียวกัน
ห้ามใช้ `model_config = ConfigDict(from_attributes=True)` แล้วโยน ORM model เข้าไปตรง ๆ
โดยไม่ตรวจว่ามีฟิลด์อะไรบ้าง

## 2.3 โครงสร้างข้อมูลที่มีอยู่แล้ว

ข่าวดี: **ตาราง `lost_claims` สร้างไว้แล้วใน migration `20260812_0003` พร้อม index ครบ
→ ไม่ต้องเขียน migration ใหม่เลย**

`app/models/lost_found.py` → คลาส `LostClaim`:

| คอลัมน์ | ชนิด | ความหมาย |
|---|---|---|
| `id` | UUID | primary key |
| `found_item_id` | UUID FK → `lost_items.id` | ประกาศที่ถูกอ้างสิทธิ์ (มี index) |
| `claimant_name` | String(150) | ชื่อผู้อ้างสิทธิ์ |
| `claimant_email` | String(255) | อีเมลผู้อ้างสิทธิ์ (มี index) |
| `proof_detail` | Text | คำอธิบายที่ใช้พิสูจน์ว่าเป็นเจ้าของ |
| `status` | enum `ClaimStatus` | สถานะคำร้อง (มี index) |
| `reviewed_by` | UUID FK → `staff.id` | clerk ที่ตรวจ (🔵 staff เขียน) |
| `review_note` | Text | บันทึกของ clerk (🔵 staff เขียน) |
| `created_at` / `updated_at` | timestamptz | เวลาสร้าง / แก้ล่าสุด |

**สิ่งที่ยังไม่มีเลย:** schema, service, endpoint — ต้องสร้างใหม่ทั้งหมด

## 2.4 State machine ของ claim

`ClaimStatus` มี 4 ค่า:

```
      guest ยื่นคำร้อง
            │
            ▼
       ┌─────────┐    clerk เทียบแล้วไม่ตรง     ┌──────────┐
       │ PENDING │───────────────────────────▶│ REJECTED │  (จบ)
       └────┬────┘                             └──────────┘
            │ clerk เทียบแล้วตรง
            ▼
       ┌──────────┐  ◀── ตรงนี้ guest ถึงจะเห็นว่าไปรับของที่ไหน
       │ APPROVED │
       └────┬─────┘
            │ clerk ยืนยันว่าส่งมอบของแล้ว
            ▼
       ┌───────────┐
       │ COMPLETED │  (จบ) — และประกาศต้นทางเปลี่ยนเป็น CLAIMED
       └───────────┘
```

⚠️ **กฎการแบ่งความรับผิดชอบที่ห้ามละเมิด:**

| ใคร | ทำอะไรได้ |
|---|---|
| 🟢 guest | **สร้าง** claim ใหม่ที่สถานะ `PENDING` เท่านั้น และ **อ่าน** สถานะของตัวเอง |
| 🔵 clerk | เปลี่ยนสถานะทุกกรณี (`PENDING` → `APPROVED`/`REJECTED` → `COMPLETED`) |

**guest backend ต้องไม่มีโค้ดที่เขียน `status` เป็นค่าอื่นนอกจาก `PENDING` เลยแม้แต่บรรทัดเดียว**

## 2.5 🟢 Endpoint ที่ต้องสร้าง (2 ตัว)

### Endpoint 1: ยื่นคำร้อง

```
POST /api/v1/guest/found-items/{item_code}/claims
```

**รับเข้า** — schema ใหม่ `GuestClaimCreate`:

```python
claimant_name: str        # min_length=1, max_length=150
claimant_email: EmailStr  # normalize เป็นตัวพิมพ์เล็ก
proof_detail: str         # min_length=1, max_length=2_000
```

> 💡 ใช้ `field_validator` แบบเดียวกับ `GuestItemCreateBase` ที่มีอยู่แล้ว
> โดยเฉพาะ `normalize_email` ที่ทำ `.strip().lower()` — **สำคัญมาก** เพราะตอนค้นหา
> ภายหลังต้องเทียบด้วยรูปแบบเดียวกัน ไม่งั้นคนกรอก `A@B.com` จะหาคำร้องตัวเองไม่เจอ

**คืนกลับ** — `GuestClaimCreatedResponse`:

```python
id: UUID
item_code: str
status: ClaimStatus     # เป็น "pending" เสมอ
message: str            # เช่น "รับคำร้องแล้ว กรุณารอเจ้าหน้าที่ตรวจสอบ"
```

**กฎการตรวจสอบ ตามลำดับ:**

| # | ตรวจอะไร | ไม่ผ่านแล้วตอบอะไร | เหตุผล |
|---|---|---|---|
| 1 | ประกาศมีอยู่ + `report_type == FOUND` + `status == APPROVED` + `deleted_at IS NULL` | 404 ข้อความกลาง ๆ | claim ได้เฉพาะ "ของที่พบ" ที่อนุมัติแล้ว ⚠️ ถ้าปล่อยให้ claim ของ `PENDING` ได้ = เปิดเผยว่ามีประกาศที่ยังไม่อนุมัติอยู่ |
| 2 | อีเมลนี้ยังไม่มี claim สถานะ `PENDING` ค้างอยู่บนประกาศเดียวกัน | 409 Conflict | กันยิงซ้ำ และกัน clerk สับสนว่าจะตรวจอันไหน |
| 3 | บันทึกลง DB สำเร็จ | 500 | ใช้ error class เดิม `ItemPersistenceError` |

⚠️ **ข้อ 1 ต้องตอบ 404 ด้วยข้อความเดียวกันทุกกรณี** ไม่ว่าจะเป็น "ไม่มีรหัสนี้" หรือ
"มีแต่ยังไม่อนุมัติ" หรือ "เป็นประกาศของหายไม่ใช่พบของ" — ถ้าแยกข้อความ คนร้ายจะใช้
ความต่างนั้นตรวจสอบว่าประกาศไหนมีอยู่จริงบ้าง เทคนิคนี้เรียกว่า **information leak
ผ่าน error message**

### Endpoint 2: ติดตามสถานะคำร้อง

```
GET /api/v1/guest/found-items/{item_code}/claims?claimant_email=...
```

**ทำไมออกแบบแบบนี้:** guest ไม่มีบัญชี จึงพิสูจน์ตัวตนด้วย **shared secret** (ความลับร่วม)
คือคู่ `item_code` + `claimant_email` — ต้องรู้ทั้งสองอย่างถึงจะดูได้
เป็นรูปแบบเดียวกับที่ใช้ในโหมดติดตามประกาศ (`GET /guest/lost-items/{item_code}?reporter_email=`)
ที่ทำไว้แล้ว ทำให้ frontend เรียนรู้ครั้งเดียวใช้ได้ทั้งระบบ

ข้อดีเพิ่ม: **ไม่ต้องสร้างรหัสติดตามใหม่และไม่ต้อง migration** เพราะ guest รู้ `item_code`
อยู่แล้วจากตอนที่เขากดยื่นคำร้อง

**คืนกลับ** — `list[GuestClaimResponse]`:

```python
id: UUID
item_code: str
claimant_name: str
status: ClaimStatus
created_at: datetime
updated_at: datetime
pickup_location: str | None   # ⚠️ ดูหัวข้อ 2.6
```

⚠️ **ห้ามคืน `review_note`** — เป็นบันทึกภายในที่ clerk เขียนให้ clerk ด้วยกันอ่าน
อาจมีข้อความอย่าง "รายนี้น่าสงสัย เคยยื่นซ้ำ 3 ครั้ง" ซึ่งไม่ได้เขียนมาให้ guest อ่าน
ถ้าอยากบอกเหตุผลการปฏิเสธจริง ๆ ต้องเพิ่มฟิลด์ใหม่ที่ clerk ตั้งใจเขียนให้ guest อ่านโดยเฉพาะ
(🔵 เป็นงานฝั่ง staff)

## 2.6 `custody_location` — ข้อมูลที่ปลดล็อกด้วยการอนุมัติ

นี่คือจุดที่ออกแบบให้สวยได้ และแก้ปัญหาที่ค้างอยู่ไปพร้อมกัน

`custody_location` (จุดรับฝากของ) **ไม่ควรให้คนทั่วไปเห็น** เพราะเป็นข้อมูลที่บอกว่า
ของมีค่าอยู่ที่ไหน แต่ **ผู้ที่ claim ผ่านแล้วจำเป็นต้องรู้** เพื่อไปรับของ

```
สถานะ claim          pickup_location ที่คืน
─────────────────────────────────────────────
PENDING           →  None   (ยังไม่รู้ว่าเป็นเจ้าของจริงไหม)
REJECTED          →  None   (ไม่ใช่เจ้าของ)
APPROVED          →  ค่าจริงของ item.custody_location  ← ปลดล็อกตรงนี้
COMPLETED         →  None   (รับของไปแล้ว ไม่ต้องรู้แล้ว)
```

🟢 ทำใน service ตอนประกอบ response:

```
ถ้า claim.status == ClaimStatus.APPROVED:
    pickup_location = item.custody_location
มิฉะนั้น:
    pickup_location = None
```

การออกแบบแบบนี้เรียกว่า **need-to-know basis** — เปิดเผยข้อมูลเฉพาะกับคนที่จำเป็นต้องรู้
ในเวลาที่จำเป็นต้องรู้เท่านั้น

## 2.7 🟢 ไฟล์ที่ต้องสร้าง/แก้

| ไฟล์ | สร้างใหม่? | ทำอะไร |
|---|---|---|
| `app/schemas/lost_found_claim.py` | ✅ ใหม่ | `GuestClaimCreate`, `GuestClaimCreatedResponse`, `GuestClaimResponse` |
| `app/services/lost_found_claim.py` | ✅ ใหม่ | `create_guest_claim()`, `list_guest_claims()` + error class เฉพาะทาง |
| `app/api/v1/endpoints/found_item.py` | แก้ | เพิ่ม 2 route (prefix `/guest/found-items` มีอยู่แล้ว ไม่ต้องแตะ router.py) |
| `app/api/v1/router.py` | ❌ ไม่ต้องแตะ | route ใหม่อยู่ใต้ prefix เดิม |
| `migrations/` | ❌ ไม่ต้องแตะ | ตาราง `lost_claims` มีครบแล้ว |
| `tests/test_guest_lost_found_claim.py` | ✅ ใหม่ | เทสต์ตามหัวข้อ 2.9 |

**Error class ที่ควรสร้าง** (ทำตามแบบแผนเดิมใน `services/lost_found.py`
ที่นิยาม exception ของตัวเองแล้วให้ endpoint แปลงเป็น HTTP status):

```python
class ClaimTargetNotFoundError(LookupError):   # → 404
    """ไม่พบประกาศพบของที่อนุมัติแล้วตามรหัสนี้."""

class DuplicateClaimError(ValueError):          # → 409
    """อีเมลนี้มีคำร้องที่ยังรอตรวจสอบบนประกาศเดียวกันอยู่แล้ว."""
```

> 💡 **ทำไมต้องสร้าง exception เอง แทนที่จะ `raise HTTPException` ใน service?**
> เพราะ service ไม่ควรรู้จัก HTTP — มันเป็น business logic ล้วน ๆ ถ้าวันหนึ่งอยากเรียก
> `create_guest_claim()` จาก background job หรือ CLI ก็ยังใช้ได้ การผูก HTTP ไว้ที่ชั้น
> endpoint เท่านั้นเรียกว่า **separation of concerns** (แยกความรับผิดชอบ)

## 2.8 ฟีเจอร์ที่ **ยังไม่ทำ** ในเฟสนี้ (ตัดสินใจแล้ว)

| ฟีเจอร์ | ทำไมยังไม่ทำ |
|---|---|
| แนบรูปเป็นหลักฐาน | ตาราง `images` มี `CheckConstraint` ชื่อ `image_parent_check` ที่บังคับว่ารูปต้องสังกัด `request_id` **หรือ** `lost_item_id` อย่างใดอย่างหนึ่ง — ไม่รองรับ claim ต้องทำ migration แก้ constraint ก่อน |
| guest ยกเลิก claim ของตัวเอง | เพิ่ม endpoint + สถานะใหม่ ยังไม่จำเป็นในเฟสแรก |
| แจ้งเตือนทางอีเมลเมื่อสถานะเปลี่ยน | โปรเจกต์ยังไม่มีระบบส่งอีเมลเลย ตาราง `notifications` ที่มีอยู่ผูกกับ `staff_id` ใช้กับ guest ไม่ได้ → guest ต้อง poll endpoint ติดตามเอง |
| จำกัดจำนวนครั้งที่ยิงได้ (rate limiting) | ⚠️ โปรเจกต์**ยังไม่มี rate limiting เลยทั้งระบบ** ดูหัวข้อ 5 |

## 2.9 🟢 เทสต์ที่ควรเขียน

**กลุ่มที่ 1 — ยื่นคำร้อง (happy path):**
1. ประกาศ `FOUND` ที่ `APPROVED` → ยื่น claim → 201 และ `status == "pending"`
2. แถวใน `lost_claims` ถูกสร้างจริง พร้อม `claimant_email` ที่ normalize เป็นตัวพิมพ์เล็กแล้ว

**กลุ่มที่ 2 — กฎการตรวจสอบ:**
3. ยื่น claim บนประกาศที่ยัง `PENDING` → 404
4. ยื่น claim บนประกาศประเภท `LOST` → 404
5. ⚠️ **assert ว่า response ของข้อ 3 และ 4 เหมือนกันเป๊ะ** ทั้ง status code และ body
   ← เทสต์ตัวนี้คือตัวที่กันไม่ให้ information leak หลุดกลับมาในอนาคต
6. ยื่นซ้ำด้วยอีเมลเดิมขณะที่ยังมี `PENDING` ค้าง → 409
7. `proof_detail` ว่าง / `claimant_email` ผิดรูปแบบ → 422

**กลุ่มที่ 3 — ติดตามสถานะ:**
8. ส่ง `claimant_email` ถูก → 200 เห็น claim ของตัวเอง
9. ส่งอีเมลคนอื่น → ไม่เห็น claim ของคนแรก (คืน list ว่าง)
10. ⚠️ ทุก response **ต้องไม่มี** `private_verification_detail` และ `review_note`

**กลุ่มที่ 4 — `pickup_location`:**
11. claim สถานะ `PENDING` → `pickup_location` เป็น `None`
12. เปลี่ยนสถานะเป็น `APPROVED` ในเทสต์โดยตรง → `pickup_location` มีค่าจริง

> 💡 ข้อ 12 ต้องเปลี่ยนสถานะเองในเทสต์ เพราะ **ยังไม่มี endpoint ของ staff ให้เรียก**
> ทำแบบเดียวกับที่เทสต์เดิมใช้ฟังก์ชัน `approve_item()` ใน `test_guest_lost_found.py`
> คือเปิด session แล้วแก้ค่าตรง ๆ แล้ว commit

---

# ส่วนที่ 3: ฝั่ง staff (อธิบายเพื่อให้เข้าใจภาพรวม)

> 🔵 **ส่วนนี้ไม่ใช่งานของฉัน** อ่านเพื่อให้รู้ว่าข้อมูลที่เราเก็บไว้ถูกเอาไปใช้ทำอะไรต่อ
> และเพื่อให้ออกแบบข้อมูลฝั่ง guest ได้ถูกต้องตั้งแต่แรก

## 3.1 clerk ทำอะไรบ้าง

**งานที่ 1 — ตรวจประกาศ (moderation)**
เปิดดูประกาศที่ `PENDING` → ดูว่าเนื้อหาเหมาะสม ไม่ใช่สแปม รูปไม่มีปัญหา
→ กดอนุมัติ (`APPROVED`) หรือปฏิเสธ (`REJECTED`) พร้อมเขียน `review_note`

**งานที่ 2 — ตัดสิน claim**
เปิดดู claim ที่ `PENDING` แล้วเห็นข้อมูล 2 ชุดวางเทียบกัน:

```
┌────────────────────────────────┬────────────────────────────────┐
│ ผู้พบเขียนไว้ (ความลับ)          │ ผู้อ้างสิทธิ์เขียนมา              │
│ private_verification_detail    │ proof_detail                   │
├────────────────────────────────┼────────────────────────────────┤
│ "ข้างในมีบัตรนักศึกษาชื่อ ก.     │ "ข้างในมีบัตรนักศึกษาของฉัน    │
│  และรูปสุนัขพันธุ์ชิวาวา"         │  กับรูปหมาชิวาวาที่เลี้ยงไว้"    │
└────────────────────────────────┴────────────────────────────────┘
                    ↓
              ตรงกัน → APPROVED
```

**งานที่ 3 — ยืนยันการส่งมอบ**
เมื่อผู้อ้างสิทธิ์มารับของจริง → กดยืนยัน → claim เป็น `COMPLETED`
และประกาศต้นทางเปลี่ยนเป็น `CLAIMED`

## 3.2 สถานะปัจจุบันของฝั่ง staff

**มีแล้ว** (`app/api/v1/endpoints/lost_found_clerk.py` + `services/lost_found_clerk.py`):

| endpoint | ทำอะไร |
|---|---|
| `GET /api/v1/lost-found/pending-found-items` | ดูรายการของที่พบซึ่งรอตรวจ |
| `GET /api/v1/lost-found/found-items/{item_id}` | ดูรายละเอียดหนึ่งรายการ |

ทั้งคู่ป้องกันด้วย dependency `ClerkStaff` ซึ่งตรวจ JWT token แล้วเช็คว่า
`role == StaffRole.CLERK` ไม่ใช่ก็ตอบ 403

**ยังไม่มีเลย:**
- ❌ endpoint อนุมัติ / ปฏิเสธประกาศ (เปลี่ยน `status`)
- ❌ endpoint ทั้งหมดที่เกี่ยวกับ claim (ดูรายการ, อนุมัติ, ปฏิเสธ, ยืนยันส่งมอบ)
- ❌ การเปลี่ยนประกาศเป็น `CLAIMED`

## 3.3 🔗 Dependency ที่กระทบงานของฉันโดยตรง

> ⚠️ **เพราะยังไม่มี endpoint อนุมัติของ staff ประกาศทุกใบจึงค้างที่ `PENDING` ตลอดไป**
> ผลคือ list endpoint สาธารณะคืนค่าว่างเสมอ และ **ไม่มีทางทดสอบ flow claim แบบครบวงจร
> ผ่าน API จริงได้เลย**

**สิ่งนี้ไม่บล็อกการพัฒนาของฉัน** เพราะเทสต์สามารถเปลี่ยนสถานะเองในฐานข้อมูลได้โดยตรง
(เทสต์เดิมทำแบบนี้อยู่แล้วในฟังก์ชัน `approve_item()`)

**แต่ต้องสื่อสารกับทีมให้ชัด** ว่าเมื่อ staff ทำ moderation endpoint เสร็จเมื่อไหร่
ต้องกลับมาทดสอบ integration ร่วมกันอีกรอบ

## 3.4 🔵 สิ่งที่ต้องบอกทีม staff ให้รู้

เมื่อทีม staff มาทำส่วน claim ต้องรู้ 3 ข้อนี้ เพราะมันผูกกับข้อมูลที่ฝั่งเราสร้าง:

1. **เมื่ออนุมัติ claim หนึ่งอัน ควรจัดการ claim อื่นบนประกาศเดียวกันด้วย**
   เพราะของมีชิ้นเดียวแต่มีคนอ้างสิทธิ์ได้หลายคน ควรปฏิเสธที่เหลืออัตโนมัติ
   ไม่งั้นจะมี claim ค้าง `PENDING` ตลอดกาลและ guest จะรอเก้อ

2. **การเปลี่ยน claim เป็น `COMPLETED` ต้องเปลี่ยน `LostItem.status` เป็น `CLAIMED` ด้วย
   ใน transaction เดียวกัน** ไม่งั้นข้อมูลสองตารางจะไม่ตรงกัน (เรียกว่า data inconsistency)

3. **ควรบันทึกประวัติการเปลี่ยนสถานะ** ตอนนี้มีตาราง `lost_item_history` สำหรับประกาศ
   แต่ **ไม่มีตารางประวัติสำหรับ claim** ถ้าต้องการ audit trail ต้องเพิ่ม migration ใหม่

---

# ส่วนที่ 4: ลำดับการลงมือ

ทำเรียงตามนี้ แต่ละขั้นจบแล้วรันเทสต์ให้เขียวก่อนขึ้นขั้นถัดไป
หลักการคือ **แก้ทีละชั้น ชั้นล่างก่อนชั้นบน** เพื่อให้รู้ทันทีว่าถ้าพังคือพังตรงไหน

### เฟส 1 — Search (เล็ก ทำก่อนให้ชิน)

| # | ทำอะไร | ไฟล์ |
|---|---|---|
| 1 | เอา `custody_location` ออกจาก list response | `schemas/lost_found_item.py`, `services/lost_found.py` |
| 2 | เพิ่ม parameter `days` ใน service | `services/lost_found.py` |
| 3 | เพิ่ม query param `days` + ขยาย `limit` ใน 2 endpoint | `endpoints/lost_item.py`, `endpoints/found_item.py` |
| 4 | เขียนเทสต์ตามหัวข้อ 1.6 | `tests/test_guest_lost_found.py` |

### เฟส 2 — Claim (ใหญ่กว่า)

| # | ทำอะไร | ไฟล์ |
|---|---|---|
| 5 | สร้าง schema ทั้ง 3 ตัว | `schemas/lost_found_claim.py` (ใหม่) |
| 6 | สร้าง service `create_guest_claim()` + error class | `services/lost_found_claim.py` (ใหม่) |
| 7 | ต่อ endpoint `POST .../claims` | `endpoints/found_item.py` |
| 8 | เขียนเทสต์กลุ่ม 1-2 → **ต้องเขียวก่อนไปต่อ** | `tests/test_guest_lost_found_claim.py` (ใหม่) |
| 9 | สร้าง service `list_guest_claims()` + logic `pickup_location` | `services/lost_found_claim.py` |
| 10 | ต่อ endpoint `GET .../claims` | `endpoints/found_item.py` |
| 11 | เขียนเทสต์กลุ่ม 3-4 | `tests/test_guest_lost_found_claim.py` |

### คำสั่งที่ใช้บ่อย

```bash
cd Backend
.venv/bin/python -m pytest tests/ -q                          # รันเทสต์ทั้งหมด
.venv/bin/python -m pytest tests/test_guest_lost_found_claim.py -v   # รันเฉพาะไฟล์
docker compose up                                             # รันระบบจริง
# เปิด http://localhost:8000/docs เพื่อดู OpenAPI แล้วลองยิง endpoint ได้เลย
```

---

# ส่วนที่ 5: ⚠️ ของค้างที่รู้อยู่แล้ว (ไม่บล็อกงานนี้ แต่ต้องรู้)

รายการนี้มาจากการรีวิวโค้ดครั้งก่อน ยังไม่ได้แก้ ไม่จำเป็นต้องแก้ในเฟสนี้
แต่ควรรู้ไว้ และ**ต้องแจ้งทีมก่อนขึ้น production**

### ต้องแก้ก่อนขึ้น production

| ประเด็น | ความเสี่ยง |
|---|---|
| **ไม่มี rate limiting ทั้งระบบ** | endpoint ติดตามสถานะและยื่น claim ยิงได้ไม่จำกัด → ใช้ไล่เดาอีเมลผู้แจ้งได้ และ spam claim ได้ ควรจำกัดต่อ IP |
| **ไม่มีระบบกู้ `item_code`** | guest ทำรหัสหาย = ติดตามคำร้องตัวเองไม่ได้ตลอดไป ทางแก้คือส่งอีเมลยืนยันตอนแจ้ง (ต้องสร้างระบบส่งอีเมลก่อน) |
| **จำกัดขนาดไฟล์อัปโหลดสายเกินไป** | `images.py` เช็คขนาดหลังจาก Starlette buffer ไฟล์ลงดิสก์ครบแล้ว ควรเช็ค `Content-Length` หรือจำกัดที่ reverse proxy |

### คุณภาพโค้ด

| ประเด็น | ผลกระทบ |
|---|---|
| `except SQLAlchemyError` ใน `create_guest_item` แคบเกินไป | ถ้าเกิด exception ที่ไม่ใช่ของ SQLAlchemy หลังอัปโหลดรูปแล้ว จะเหลือไฟล์กำพร้าใน R2 โดยไม่มีใครลบ |
| `_make_item_code` ใช้เวลา UTC ไม่ใช่เวลาไทย | รหัสอาจแสดงวันที่ของเมื่อวานสำหรับคนไทยช่วงใกล้เที่ยงคืน |
| `_make_item_code` ไม่มีการ retry เมื่อรหัสชนกัน | ถ้าชนจะขึ้น 500 แบบไม่มีคำอธิบาย |
| `search` ไม่ได้ escape `%` และ `_` | guest พิมพ์ `%` → กลายเป็น wildcard → คืนทุกแถว (ถ้าเลิกใช้ `?search=` แล้วปัญหานี้ลดความสำคัญลง) |
| `tests/conftest.py` ไม่ได้เปิด `PRAGMA foreign_keys=ON` | SQLite ในเทสต์ไม่บังคับ foreign key → บั๊กเรื่องลำดับการ INSERT มองไม่เห็นจากเทสต์ |
| ไม่มี job จัดการ `expires_at` / `archived_at` | ประกาศเก่าไม่หายไปเอง ข้อมูลโตทางเดียวไม่มีเพดาน → กระทบสมมติฐาน "ข้อมูลน้อย" ของสถาปัตยกรรม search ในระยะยาว |
| ความยาวฟิลด์ประกาศซ้ำ 2 ที่ | `forms.py` (`Form(max_length=...)`) กับ `schemas/` (`Field(max_length=...)`) ต้องแก้พร้อมกันเสมอ ไม่งั้นเพี้ยน |

---

## ภาคผนวก: แผนที่ไฟล์ที่เกี่ยวข้อง

```
Backend/app/
├── api/
│   ├── dependencies.py              DbSession, ObjectStorageClient, ClerkStaff
│   └── v1/
│       ├── router.py                รวม endpoint ทุกตัว
│       ├── forms.py                 แปลง multipart form → schema
│       └── endpoints/
│           ├── lost_item.py         🟢 guest: ประกาศของหาย
│           ├── found_item.py        🟢 guest: ประกาศพบของ (+ claim จะมาอยู่ที่นี่)
│           └── lost_found_clerk.py  🔵 staff: ตรวจสอบประกาศ
├── models/
│   ├── enums.py                     LostStatus, LostType, ClaimStatus, StaffRole
│   └── lost_found.py                LostItem, LostItemHistory, LostClaim
├── schemas/
│   ├── lost_found_item.py           🟢 schema ฝั่ง guest
│   ├── lost_found_claim.py          🟢 ← สร้างใหม่
│   └── lost_found_clerk.py          🔵 schema ฝั่ง staff
└── services/
    ├── lost_found.py                🟢 business logic ประกาศ
    ├── lost_found_claim.py          🟢 ← สร้างใหม่
    ├── lost_found_clerk.py          🔵 business logic ฝั่ง staff
    ├── images.py                    ตรวจ + แปลงรูปเป็น WebP
    └── object_storage.py            เชื่อม Cloudflare R2
```
